if [ "$XDG_SESSION_DESKTOP" = "budgie-desktop" ] || [ "$XDG_SESSION_DESKTOP" = "deepin" ] || [ "$XDG_SESSION_DESKTOP" = "xfce" ] || [ "$XDG_SESSION_DESKTOP" = "i3" ] || [ "$XDG_SESSION_DESKTOP" = "fluxbox" ] || [ "$XDG_SESSION_DESKTOP" = "blackbox" ]; then
    export QT_QPA_PLATFORMTHEME=gtk2
    export $(/usr/bin/gnome-keyring-daemon --start --components=ssh,secrets,pkcs11)
fi

if [ "$XDG_SESSION_DESKTOP" = "KDE" ]; then
    export SSH_ASKPASS=/usr/bin/ksshaskpass
    eval $(ssh-agent -s)
fi

if [ "$XDG_SESSION_DESKTOP" = "i3" ] || [ "$XDG_SESSION_DESKTOP" = "fluxbox" ] || [ "$XDG_SESSION_DESKTOP" = "blackbox" ]; then
    export SSH_ASKPASS=/usr/lib/seahorse/ssh-askpass
    export TERMINAL=termite
fi

if [ -d $HOME/.bin ]; then
    if ! grep "$HOME/.bin" <<< $PATH; then
        export PATH=$HOME/.bin:$PATH
    fi
fi
